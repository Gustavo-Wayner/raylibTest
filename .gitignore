*.exe
*.dsym
main

# Ignore common build artifacts and toolchain/download outputs (added for `raylib`)
# Object and library files
*.o
*.obj
*.a
*.lib
*.dll
*.pdb
*.so
*.dylib

# Raylib build and distribution folders (ignore generated/dist artifacts, keep source)
# Raylib build and distribution folders (ignore generated/dist artifacts, keep source)
# Note: toolchains (w64devkit/emsdk) are intentionally included in the repository
# for portability on locked college machines. Keep the following entries only
# if you want to ignore generated build folders inside raylib subprojects.
/raylib/**/build/
/raylib/**/dist/
/raylib/**/zips/

# Emscripten/emsdk downloads and temporary files
# (kept out of git if large build downloads are undesirable)
# /raylib/emsdk/downloads/
# /raylib/emsdk/zips/

# w64devkit toolchain binaries and generated runtime files
# (included in repo for portability; uncomment to ignore)
# /raylib/w64devkit/bin/
# /raylib/w64devkit/lib/
# /raylib/w64devkit/share/
# /raylib/w64devkit/x86_64-w64-mingw32/